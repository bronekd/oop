from .calculate_area import Shape
# najdu ve složce geometry a není obycejná složka je tam __init__.py obsahuje a dělá python pagaces
class Square(Shape):
    def __init__(self, name, side_length):
        super().Calculate_area()